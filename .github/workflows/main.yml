on:
  push:
    branches: [testDeploy]
name: Test deploy
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: Use Node.js 18
      uses: actions/setup-node@v2
      with:
        node-version: '18'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run build
    
    - name: 📂 Sync files
      uses: kevinpainchaud/simple-ftp-deploy-action@v1.2.1
      with:
        ftp_host: ftp.flexicontech.org
        ftp_username: test_123@flexicontech.org
        ftp_password: RKUCi14zhh#2
        dist_target_dir: ""
        local_source_dir: "dist"
        delete: "true"
